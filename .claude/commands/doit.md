description: Execute development tasks from arguments with allowed-tools: Bash(npx:*), Bash(npm:*), Bash(pip:), Bash(mkdir:*), Move, Read, Edit, Write, Delete, Grep, Glob, Ripgrep, WebFetch, WebSearch
argument-hint: [ task description ]

Execute tasks: $ARGUMENTS

Process:
1. Analyze task requirements
2. Break into small safe operations
3. Execute commands as needed
4. Provide execution summary
