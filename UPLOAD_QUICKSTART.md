# Быстрый старт: Загрузка изображений в Cloudinary

## ⚡ Установка за 3 шага

### 1. Получите учетные данные Cloudinary

1. Зарегистрируйтесь на https://cloudinary.com/ (бесплатный план)
2. В Dashboard скопируйте:
   - Cloud Name
   - API Key
   - API Secret

### 2. Настройте .env

```bash
cd apps/backend
cp .env.example .env
```

Откройте `apps/backend/.env` и заполните:

```env
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
```

### 3. Запустите серверы

**Backend:**
```bash
cd apps/backend
npm run dev
```

**Frontend (в новом терминале):**
```bash
cd apps/frontend
npm run dev
```

## 🧪 Тестирование

Откройте в браузере:
```
http://localhost:3000/test-upload
```

Попробуйте:
- ✅ Перетащить изображение
- ✅ Выбрать файл через клик
- ✅ Загрузить несколько файлов
- ✅ Удалить файл из превью
- ✅ Проверить URL загруженного изображения

## 📝 Использование в коде

```tsx
import { ImageUpload } from '@/components/upload/ImageUpload'

function MyComponent() {
  return (
    <ImageUpload
      maxFiles={1}
      folder="prompts/thumbnails"
      onUploadComplete={(results) => {
        console.log('Uploaded:', results[0].url)
      }}
    />
  )
}
```

## 📚 Полная документация

Смотрите `docs/UPLOAD_SETUP.md` для подробной информации.

## ✨ Что реализовано

- ✅ Backend NestJS модуль с Cloudinary интеграцией
- ✅ Frontend компонент с drag-and-drop
- ✅ Валидация файлов (тип, размер)
- ✅ Превью перед загрузкой
- ✅ Индикатор прогресса
- ✅ Автоматическая оптимизация изображений
- ✅ Генерация thumbnail URL
- ✅ Обработка ошибок
- ✅ Типы TypeScript для всего стека

## 🔧 Требования

- Node.js 18+
- Аккаунт Cloudinary (бесплатный)
- PostgreSQL (для основного проекта)

## ❓ Проблемы?

1. **"Upload failed: 401"** - проверьте Cloudinary credentials в .env
2. **"Invalid file type"** - используйте только JPEG/PNG/WebP/GIF
3. **"File too large"** - максимум 10MB на файл

Подробнее в `docs/UPLOAD_SETUP.md`
